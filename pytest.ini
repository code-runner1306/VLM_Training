[pytest]
# langsmith_plugin (auto-loaded entry point) triggers a native DLL access
# violation at pytest startup on Windows; disable it so the suite can run.
addopts = -p no:langsmith_plugin
testpaths = tests
